{
  "id": "oh-my-zsh",
  "version": "1.0.0",
  "name": "Oh My Zsh",
  "description": "Installs zsh and Oh My Zsh with a configurable set of plugins and theme. External plugins (zsh-autosuggestions, zsh-syntax-highlighting, pnpm, etc.) are cloned automatically. Writes a managed .zshrc and sets zsh as the default shell.",
  "options": {
    "plugins": {
      "type": "string",
      "default": "git sudo z history colored-man-pages zsh-autosuggestions zsh-syntax-highlighting pnpm",
      "description": "Space-separated list of Oh My Zsh plugins to activate. Built-in plugins are enabled directly; the following external plugins are recognised by name and cloned automatically: zsh-autosuggestions, zsh-syntax-highlighting, zsh-completions, zsh-history-substring-search, pnpm. If this option is specified, it completely replaces the default list."
    },
    "theme": {
      "type": "string",
      "default": "robbyrussell",
      "description": "Oh My Zsh theme name (e.g. 'robbyrussell', 'agnoster', 'clean')."
    },
    "extraRcSnippets": {
      "type": "string",
      "default": "",
      "description": "Additional shell code appended after 'source oh-my-zsh.sh' in the generated .zshrc. Use this to inject project-specific customizations (NVM sourcing, direnv hooks, autosuggest config, etc.) without a separate post-create script."
    },
    "extraRcFile": {
      "type": "string",
      "default": "",
      "description": "Path to a shell file (relative to the workspace root, e.g. '.devcontainer/zshrc-extras.sh') whose contents are appended to .zshrc after 'source oh-my-zsh.sh'. Use this to keep extra snippets in a readable, syntax-highlighted file instead of a JSON string. If both extraRcSnippets and extraRcFile are provided, snippets are appended first, then the file contents."
    },
    "autosuggestStyle": {
      "type": "string",
      "default": "",
      "description": "Sets ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE if non-empty (e.g. 'fg=60'). Only takes effect when the zsh-autosuggestions plugin is active."
    },
    "autosuggestStrategy": {
      "type": "string",
      "default": "",
      "description": "Sets ZSH_AUTOSUGGEST_STRATEGY if non-empty (e.g. 'history completion'). Only takes effect when the zsh-autosuggestions plugin is active."
    }
  },
  "installsAfter": []
}

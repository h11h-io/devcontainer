{
  "id": "git-identity-from-github",
  "version": "1.0.0",
  "name": "Git Identity from GitHub",
  "description": "Automatically sets git user.name and user.email from the authenticated GitHub account at container start time. Works with GitHub Codespaces (GITHUB_TOKEN), Coder (external auth), and gh CLI.",
  "options": {
    "overwrite": {
      "type": "boolean",
      "default": false,
      "description": "If true, overwrite any existing git user.name and user.email configuration. If false (default), skip if identity is already set."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/github-cli:1"
  ],
  "postStartCommand": "configure-git-identity"
}
